#pragma once

/*
 *
 * add struct for beat
 * add function for processing 
 * possibly add options for ncurses view, most likely add to a separate header
 *
 *
 */

int start_server(char *ip, int port);



int handle_client();
